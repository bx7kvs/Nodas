import Application from "../../../core/modules/Application/Application";
import {ReflectElementProps} from "../@types/types";
import ReflectElement from "../Element";

export default class Area extends ReflectElement {
    constructor(props:ReflectElementProps) {
        super(props);
    }
}