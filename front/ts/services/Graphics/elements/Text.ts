import {ReflectElementProps} from "../@types/types";
import ReflectElement from "../Element";

export default class Text extends ReflectElement {
    constructor(props:ReflectElementProps) {
        super(props);
    }
}